apiVersion: v2
name: thalamus
description: Thalamus operator and LLM inference workloads (vLLM model servers, inference gateway, routing). Requires thalamus-crds to be installed first.
type: application
version: 0.1.0
appVersion: latest
